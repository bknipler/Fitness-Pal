<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:StepCounter.Views;assembly=StepCounter"
             x:Class="StepCounter.Views.Master">
  
  <MasterDetailPage.Master>
    <ContentPage Title="Menu">
      <StackLayout Orientation="Vertical">
        <Label Text =" "/>
        <Label Text =" "/>
        <Button Text="Goals" Command="{Binding Goals}"/>
        <Button Text="Health Indicator" Command="{Binding Health}"/>
        <Button Text="Fitness Tips" Command="{Binding Tips}"/>
      </StackLayout>
    </ContentPage>
  </MasterDetailPage.Master>
  <MasterDetailPage.Detail>
    <views:StepCountPage/>
  </MasterDetailPage.Detail>
  </MasterDetailPage>